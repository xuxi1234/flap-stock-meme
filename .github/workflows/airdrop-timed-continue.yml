name: 旧30轮空投已停用｜请使用新72轮入口
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo '旧任务已按用户要求停止，已完成16轮保留。请打开airdrop-new72.yml启动新任务。此入口不再发币。'
