# 7031 地址 GitHub Actions 手动启动任务

入口：https://github.com/xuxi1234/flap-stock-meme/actions/workflows/airdrop-snapshot0922.yml

## 本人启动步骤
1. 点击 Run workflow，分支选择 main。
2. operation 选择“启动或继续7031地址空投”。
3. 勾选本人确认，点击绿色 Run workflow。
4. 打开新运行记录，查看逐批进度和 summary。启动后由脚本按间隔续跑，无需每小时重新点击。

默认“只读检查”不会转账；推送代码和PR仅运行离线测试。没有schedule定时触发器。
需要暂停时取消本次运行；之后从同一入口重新手动启动，复用原检查点，不创建第二份空投。
交易失败、回执矛盾、钱包出现记录外交易等情况会停止，需要先核对原因，不会自动重复发送失败或不明交易。

## 固定参数
- 钱包：0x74a7D3198905C3b4BA53574C2DffEF3aa4e569aA
- BSC，chainId 56
- 蝴蝶股票：0x4b112e1ed0c0cb332d2b39e5dae3bba882f67777
- 分发合约：0x369890cb7a233be14d33fd4265b26afadce00bc0
- 7031 个地址，快照区块123329990，12个代币前600持仓合并去重
- 每地址转出0.7777枚，总转出5468.0087枚；到账按代币税费扣除，不补发
- 36批：35批各200地址，最后31地址
- 上一批链上确认时间后至少3600秒；节点、GitHub排队等可能延长
- 不设单笔BNB手续费金额上限或累计Gas预算上限；仍校验Gas参数、余额和合约代码
- 第一批发出至最后一批最少35小时，实际耗时可能更长

## 与旧任务的关系
沿用旧5557地址任务的回执核对、双RPC、串行执行和持久检查点逻辑。
共用同一钱包排队锁；旧5557任务必须全部28批成功结束后，才允许新任务开始。
不会取消或修改旧任务。如旧任务失败或未完成，先处理旧任务再启动此任务。
同一钱包期间不要执行其他发送操作，否则nonce核验会停止。
新任务和已发布钱包页面共用同一批次标识，以防同一批重复到账；不要同时使用两种方式。
如果已用钱包页面发出部分批次而GitHub检查点没有记录，脚本会停止，不会自动补发。

## 账户配置与状态
脚本在用户手动启动后的执行步骤使用既有 FLAP_MINT_PRIVATE_KEY 和 FLAP_BSC_RPC_URL。
代码提交、离线测试和默认只读检查不会读取签名私钥。
RPC沿用仓库配置的Alchemy BSC主网付费RPC，并与第二节点核对。
状态保存于 automation/airdrop-snapshot0922-ledger/journal.json；请勿删除或重置此分支。
保存公开交易参数和哈希后才广播；不保存私钥或原始签名交易。
运行附件包含summary、名单、实际到账CSV和检查点。

## 验证范围
已通过离线测试：名单哈希、7031地址、36批和末批31地址、精确金额、
与钱包页面相同batchId、无累计Gas上限、60分钟间隔、显式手动确认、
持久化失败禁止广播、旧任务完成门槛、31条末批实际到账事件、检查点篡改拦截。
测试不连接真实钱包、不签名、不广播；本次交付没有代替用户启动链上任务。
