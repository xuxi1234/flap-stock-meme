# 新72轮空投

用户已要求停止原30轮任务，另外开始72轮。旧任务已完成16轮、3200地址、320枚，这些链上转账无法撤回，不计入新任务的72轮；历史Gas仍计入累计预算。

新任务固定为72轮，每轮200个随机新地址，每地址0.1枚，共14400地址、1440枚。名单为固定任务ID生成，排除此前20轮与30轮任务的所有名单，不会因重跑而生成第二组名单。随机地址不代表真实用户。

## 手动入口

https://github.com/xuxi1234/flap-stock-meme/actions/workflows/airdrop-new72.yml

Run workflow → main → 启动或继续新72轮 → 勾选确认 → 绿色Run workflow。

默认是只读检查，不接触私钥。执行使用既有Secret `FLAP_MINT_PRIVATE_KEY`，对应钱包 `0x74a7D3198905C3b4BA53574C2DffEF3aa4e569aA`，无需重新填写。收件人来自固定新名单；不改变任何营收地址。固定代币 `0x4b112e1ed0c0cb332d2b39e5dae3bba882f67777`，批量工具 `0x369890cb7a233be14d33fd4265b26afadce00bc0`。

首轮尽快发送，随后每轮至少距上轮确认1200秒，不补发密集轮次。72个串行任务，单任务最多40分钟，全部最快23小时40分钟，节点与GitHub排队会延长时间。启动后可关闭浏览器；失败会停止后续队列，保留已有哈希，重新运行先核对旧交易。不要同时启动其他使用这个钱包的任务。

## 预算

不收工具费，value始终为0，只付Gas。旧任务确认后的累计费用为0.053882222469141262 BNB，新任务可用累计剩余额度0.046117777530858738 BNB。原累计0.1 BNB上限保留，包含此前任务与全部Gas；仅原已确认自有钱包调拨交易的本金单列，Gas从不排除。

按照已发轮次实际Gas估计，新72轮约需0.0455 BNB，此外还需清零原授权和授权1440枚，预算余量很小。每筆签名前会按gasLimit×gasPrice预留最大费用，价格上升或预算不足会停止。该估算不保证足够完成全部72轮，不会通过重置记录提高预算。

## 进度与恢复

新进度分支：`automation/airdrop-72x200-ledger`，旧分支保留、active=false。新任务每轮先要求旧分支仍暂停且旧交易记录与16轮快照一致，再核对49笔历史交易、双RPC余额/nonce/完成标志及每个地址实际到账。

对GitHub GET的短暂故障有限重试；PUT返回409/500或响应丢失时先重读远程内容：与目标一致视为已保存，与原内容一致才有限重试，其他变更一律停止。签名广播不做盲目重试。每次必须先持久化固定交易和哈希，再广播。

72轮全部完成后自动停止，不会创建第73轮。每轮提供summary、原名单、已核实逐地址结果CSV和公开检查点；不写入私钥或原始签名交易。
