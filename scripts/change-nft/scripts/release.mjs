import fs from 'node:fs';import path from 'node:path';import {keccak256} from 'ethers';import {compile,root} from './compile.mjs';
const out=path.resolve(root,'../dist/nft');const sha=fs.readFileSync(path.join(out,'manifest-sha256.txt'),'utf8').trim();if(!/^[0-9a-f]{64}$/.test(sha))throw Error('Bad manifest');const {artifact}=compile();let runtime=artifact.evm.deployedBytecode.object;for(const refs of Object.values(artifact.evm.deployedBytecode.immutableReferences))for(const {start,length}of refs){if(length!==32)throw Error('Unexpected immutable');runtime=runtime.slice(0,start*2)+sha+runtime.slice((start+length)*2);}
const config={chainId:56,rpc:'https://bsc-dataseed.bnbchain.org',revenue:'0x23898F0866E4D5fa5C9B97645239eD635EA93cDC',manifestHash:'0x'+sha,runtimeHash:keccak256('0x'+runtime),deploymentUrl:'https://raw.githubusercontent.com/xuxi1234/flap-stock-meme/main/scripts/change-nft/deployment.json',abi:artifact.abi};
fs.writeFileSync(path.join(out,'config.json'),JSON.stringify(config));fs.writeFileSync(path.join(root,'collection-release.json'),JSON.stringify({count:7777,manifestSha256:sha,runtimeHash:config.runtimeHash},null,2));console.log('Release pinned:',sha);
