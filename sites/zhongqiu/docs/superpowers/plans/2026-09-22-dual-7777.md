# 蝴蝶中秋双系列正式发行

用户已批准：嫦娥、玉兔各 7777 枚，单价 0.001 BNB，BNB Chain，现有原画组合衍生；收入地址 0x23898F0866E4D5fa5C9B97645239eD635EA93cDC，部署签名地址 0x74a7D3198905C3b4BA53574C2DffEF3aa4e569aA。前端 Vercel，域名 zhongqiu.sh。不得把尚未部署的功能显示为可交易。

## 实现与验收
1. 确认域名、部署权限、现有原画与 GitHub 签名工作流。
2. 确定性生成两个系列各 7777 张独立 WebP 和元数据。人物不改色、不变形；装饰组合互异；故事披露为程序化组合叙事。校验图片哈希、组合唯一性、元数据数量。保留原始图片和构建脚本。
3. 两个 ERC721 合约使用已验证的 Chainlink VRF 组件，价格、供给和收益地址固定。异步随机分配，按请求顺序结算，避免回调先后影响分配。随机服务停机时队列会等待，不伪造随机数或重新抽签。
4. 市场仅支持这两个合约；验证订单签名、所有权、批准、价格、到期和撤销，购买原子交割，平台手续费为零。
5. 测试超量、错误付款、重复编号、回调权限、订单重放、撤销、付款失败和钱包变更。主网部署前发布内容寻址的资产与 manifest。
6. 通过已有 GitHub Secret 在受控部署作业中签名，私钥不离开 runner。检查共享账户 nonce、防止重复部署。任何平台要求的人工批准保留。
7. 主网小额验证、合约源码验证、填入真实地址后才开放铸造。官网显示真实链上状态，提供浏览器交易链接。

## 交付记录
尚未部署新合约；所有发布状态以链上交易回执和实际网址检查为准。
